import { Wrapper } from '@/features/components/layout';
import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const Index = () => {
    return (
        <Wrapper>
            <Text style={{padding:16}}>Settings</Text>
        </Wrapper>
    );
}

const styles = StyleSheet.create({})

export default Index;
