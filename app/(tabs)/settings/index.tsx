import { Wrapper } from '@/components/layout';
import React from 'react';
import { StyleSheet, Text } from 'react-native';

const Index = () => {
    return (
        <Wrapper>
            <Text style={{padding:16}}>Settings</Text>
        </Wrapper>
    );
}

const styles = StyleSheet.create({})

export default Index;
